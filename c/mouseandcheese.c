#include <stdio.h>
#include <stdlib.h>
#include <string.h>


char s[][] = {"algorithm", "jazz", "peas", "chocolate", "monk", "sailboat"};


int main() {
	int sindex = (rand() % 6);
    int wordlength = strlen(s[sindex]);
	for (int i = 0; i < wordlength; i++) {
        
	}
}